const sequelize = require('sequelize');
const db = require('../config/database');
const Butaca = require('../models/Butaca');

const controller = {};

controller.CreateButaca = async function(data){
    /*try{
        console.log(data);
        Censura_pelicula.create(data);
    }catch (error){
        callback (null, error);
    }*/
};


module.exports = controller;